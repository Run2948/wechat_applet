<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.UserLevelDao">

    <resultMap type="com.platform.entity.UserLevelEntity" id="userLevelMap">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
    </resultMap>

	<select id="queryObject" resultType="com.platform.entity.UserLevelEntity">
		select
			id,
			name,
			description
		from nideshop_user_level
		where id = #{id}
	</select>

	<select id="queryList" resultType="com.platform.entity.UserLevelEntity">
		select
			id,
			name,
			description
		from nideshop_user_level
		WHERE 1=1
		<if test="name != null and name.trim() != ''">
			AND name like concat('%',#{name},'%')
		</if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from nideshop_user_level
		WHERE 1=1
		<if test="name != null and name.trim() != ''">
			AND name like concat('%',#{name},'%')
		</if>
	</select>
	 
	<insert id="save" parameterType="com.platform.entity.UserLevelEntity" useGeneratedKeys="true" keyProperty="id">
		insert into nideshop_user_level(
			`name`,
			`description`)
		values(
			#{name},
			#{description})
	</insert>
	 
	<update id="update" parameterType="com.platform.entity.UserLevelEntity">
		update nideshop_user_level 
		<set>
			<if test="name != null">`name` = #{name}, </if>
			<if test="description != null">`description` = #{description}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from nideshop_user_level where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from nideshop_user_level where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>